{
    "$schema": "https://www.schemastore.org/package.json",
    "name": "api",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "concurrently -k -n vite,server -c blue,green \"vite\" \"php artisan serve\"",
        "lint": "echo 'no JS lint configured'",
        "test": "php artisan test"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^3.0.0",
        "tailwindcss": "^4.0.0",
        "vite": "^8.0.0"
    }
}
